[project]
name = "minipy"
version = "0.1.0"
description = "A small educational language transpiled to CPython AST"
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"